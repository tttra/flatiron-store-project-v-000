<h1><%= @category.title %></h1>

<ul>
<% @category.items.each do |item| %>
    <li><%= item.title %> | <%= number_to_currency(item.price.to_f/100) %> | <%= button_to 'Add to Cart', line_items_path(item_id: item) if user_signed_in? %></li>
<% end %>
</ul>